frontend:

lovelace:
  mode: yaml
  resources:
    - url: /local/bmw-status-card/bmw-status-card.js?v=dev-mvp-7
      type: module

cardata:

logger:
  default: info
  logs:
    custom_components.bmw_status: debug
    custom_components.cardata: debug